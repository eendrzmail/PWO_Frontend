import { AbsenceTypePipePipe } from './absence-type-pipe.pipe';

describe('AbsenceTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AbsenceTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
